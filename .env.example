# PROJECT H.Y.D.R.A. Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# =============================================================================
# NASA EARTHDATA AUTHENTICATION
# =============================================================================
# Create account at: https://urs.earthdata.nasa.gov/
# Required for satellite data access
NASA_EARTHDATA_USERNAME=your_username
NASA_EARTHDATA_PASSWORD=your_password

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SQLite database file (if not using PostgreSQL)
# DATABASE_URL=sqlite:///./hydra.db

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# Streamlit Server Settings (if needed)
# STREAMLIT_SERVER_PORT=5000
# STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Cache Directory (defaults to ./cache)
# CACHE_DIR=./cache

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# =============================================================================
# REPLIT DEPLOYMENT NOTES
# =============================================================================
# When deploying on Replit:
# 1. Add secrets via Replit Secrets tool (do NOT use .env file)
# 2. DATABASE_URL is automatically provided by PostgreSQL add-on
# 3. NASA credentials must be added manually to Secrets
#
# Access Secrets at: https://replit.com/~/secrets (for your Repl)
#